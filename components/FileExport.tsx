const FileExport = () => {
    return (
        <div className=' bg-violet-500 text-gray-50 p-3 rounded-lg flex justify-center items-center hover:bg-violet-700 hover:text-white
        cursor-pointer select-none transition duration-200'
             onClick={() => {
             }}>
            Export Data as CSV
        </div>
    )
}

export default FileExport